# Pinned against what was actually verified by installing and reading the
# source, not from memory. deepagents 0.7.14 requires langchain>=1.4, which
# is a hard break from the langchain 0.3.x line — do not mix them.
deepagents==0.7.14
langchain>=1.4,<2
langgraph>=1.2,<2
langgraph-sdk>=0.2,<1
langchain-anthropic>=1.0,<2

# The thin Zoho Desk MCP wrapper (src/pagerduty_triage/mcp_server/).
# Only needed when ZOHO_TRANSPORT=mcp; harmless otherwise.
mcp>=1.2
httpx>=0.27

# The Slack interaction endpoint served as a LangGraph custom route.
starlette>=0.37
