[pytest]
# Tests import sibling modules (`score`, and later `replay` / `run_eval`) from this
# directory, so put it on sys.path. Run with: cd tools/eval && python -m pytest tests -v
pythonpath = .
testpaths = tests
